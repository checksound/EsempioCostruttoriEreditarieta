# Esempio costruttori con ereditarietà

Utilizzo di `super()`.

Nel codice sotto il pacchetto `it.esempio.uno` la versione di partenza di `Circle` e `GraphicCircle`, mentre con `it.esempio.due` la versione finale.